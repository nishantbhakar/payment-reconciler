services:
  - type: web
    name: payment-reconciler
    runtime: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && python generate_data.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
